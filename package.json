{
  "name": "qai",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Agent QA — scénarios en intentions, rejouables sur le web et le mobile",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "playwright": "^1.62.0"
  }
}
