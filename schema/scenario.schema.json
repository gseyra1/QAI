{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qai.dev/schema/scenario.schema.json",
  "title": "QAI Scenario",
  "description": "Describes a user intention, independently of the platform. Never contains a selector, a technical identifier, or a coordinate: those live in the resolution file.",
  "type": "object",
  "required": ["id", "title", "steps"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Stable identifier of the scenario. Must never change once assigned."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "What the scenario proves, phrased from the user's point of view."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Used for filtering in CI (e.g. critical-path for the blocking pull request batch)."
    },
    "platforms": {
      "type": "array",
      "items": { "$ref": "#/$defs/platform" },
      "description": "Targeted platforms. Absent = all those configured for the project."
    },
    "given": {
      "type": "object",
      "additionalProperties": false,
      "description": "Required starting state. Declared, never built click by click.",
      "properties": {
        "fixtures": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Named data sets, defined outside the scenario and resolved per environment."
        },
        "state": {
          "type": "string",
          "description": "Named application state (e.g. anonymous-visitor, premium-account)."
        }
      }
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/step" }
    }
  },
  "$defs": {
    "platform": {
      "type": "string",
      "enum": ["web", "mobile", "ios", "android"]
    },
    "step": {
      "type": "object",
      "required": ["id"],
      "additionalProperties": false,
      "anyOf": [
        { "required": ["do"] },
        { "required": ["per_platform"] }
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9_-]+$",
          "description": "Stable anchor the cached resolution attaches to. Assigned at creation, never modified — even if the step's text is reworded."
        },
        "do": {
          "type": "string",
          "minLength": 1,
          "description": "The intention, in natural language, in the team's language. No selectors."
        },
        "per_platform": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": false,
          "description": "Rewording of the intention for a given platform, when the gesture genuinely differs. An exception to keep rare and visible. Do NOT rename to 'on': YAML 1.1 interprets that key as the boolean true.",
          "properties": {
            "web": { "type": "string" },
            "mobile": { "type": "string" },
            "ios": { "type": "string" },
            "android": { "type": "string" }
          }
        },
        "only": {
          "type": "array",
          "items": { "$ref": "#/$defs/platform" },
          "minItems": 1,
          "description": "Restricts the step to certain platforms (e.g. a hover does not exist on mobile)."
        },
        "expect": {
          "description": "Semantic assertion(s). Self-healing is never allowed to modify them.",
          "oneOf": [
            { "type": "string", "minLength": 1 },
            {
              "type": "array",
              "items": { "type": "string", "minLength": 1 },
              "minItems": 1
            }
          ]
        },
        "capture": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "type": "string" },
          "description": "Extracts values from the screen, reusable later via {{name}}. Avoids freezing test data into assertions.",
          "propertyNames": { "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$" }
        }
      }
    }
  }
}
